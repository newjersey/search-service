# Point this at a role with read-only access to the `grants` table only —
# never an application's full credentials. See src/db.ts and README.md.
GRANTS_DATABASE_URL=postgres://readonly_user:password@localhost:5432/grants
